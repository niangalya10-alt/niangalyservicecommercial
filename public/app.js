const esc=s=>(s??"").toString().replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));async function loadVehicles(){const r=await fetch("/api/vehicles"),vs=await r.json();const box=document.querySelector("#vehicles"),none=document.querySelector("#none");none.style.display=vs.length?"none":"block";box.innerHTML=vs.map(v=>`<article class="vehicle"><img src="${v.image||"/logo.svg"}" onerror="this.src='/logo.svg'" alt="${esc(v.title)}"><div class="vbody"><h3>${esc(v.title)}</h3><strong>${esc(v.price)} FCFA</strong><div class="spec">${[v.year,v.mileage,v.fuel,v.gear].filter(Boolean).map(x=>`<span>${esc(x)}</span>`).join("")}</div><p>${esc(v.description||"Disponible. Contactez-nous pour plus d'informations.")}</p><a class="btn gold" target="_blank" href="https://wa.me/22376652572?text=${encodeURIComponent("Bonjour, je suis intéressé(e) par : "+v.title+" ("+v.price+" FCFA).")}">Je suis intéressé</a></div></article>`).join("")}document.querySelector("#year").textContent=new Date().getFullYear();loadVehicles();document.querySelector("#contactForm").addEventListener("submit",async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));const r=await fetch("/api/messages",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();document.querySelector("#sent").textContent=r.ok?"Message envoyé. Nous vous répondrons rapidement.":"Erreur : "+j.error;if(r.ok)e.target.reset()});